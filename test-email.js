require('dotenv').config(); const nodemailer = require('nodemailer'); const t = nodemailer.createTransport({ service: 'gmail', auth: { user: process.env.SCHOOL_EMAIL, pass: process.env.SCHOOL_EMAIL_PASSWORD }}); t.sendMail({ from: process.env.SCHOOL_EMAIL, to: process.env.SCHOOL_EMAIL, subject: 'Test', text: 'Test' }).then(console.log).catch(console.error);
