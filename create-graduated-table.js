const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { await prisma.$executeRawUnsafe("CREATE TABLE public.graduated_students (id SERIAL PRIMARY KEY, student_code TEXT, name TEXT NOT NULL, graduation_year TEXT NOT NULL, medium TEXT NOT NULL, email TEXT, phone TEXT, created_at TIMESTAMP(6) DEFAULT CURRENT_TIMESTAMP);"); console.log('Table created'); } main().catch(console.error).finally(() => prisma.$disconnect());
